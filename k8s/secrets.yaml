# TEMPLATE ONLY — do NOT commit real secrets.
# In production, use External Secrets Operator or Sealed Secrets
# to pull from OpenBao/Vault at runtime.
apiVersion: v1
kind: Secret
metadata:
  name: tnt-engine-secrets
  namespace: tnt-engine
type: Opaque
stringData:
  TNT_DB_USER: "tnt"
  TNT_DB_PASSWORD: "REPLACE_WITH_VAULT_SECRET"
  TNT_CRYPTO_TOKEN: "REPLACE_WITH_VAULT_SECRET"
