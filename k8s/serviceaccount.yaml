apiVersion: v1
kind: ServiceAccount
metadata:
  name: tnt-engine
  namespace: tnt-engine
  labels:
    app: tnt-engine
  annotations:
    # For AWS IRSA or GCP Workload Identity — uncomment and configure:
    # eks.amazonaws.com/role-arn: arn:aws:iam::123456789:role/tnt-engine
    # iam.gke.io/gcp-service-account: tnt-engine@project.iam.gserviceaccount.com
automountServiceAccountToken: false
